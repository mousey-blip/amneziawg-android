<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <import type="org.amnezia.awg.model.ObservableTunnel" />

        <variable
            name="fragment"
            type="org.amnezia.awg.fragment.TunnelListFragment" />

        <variable
            name="rowConfigurationHandler"
            type="org.amnezia.awg.databinding.ObservableKeyedRecyclerViewAdapter.RowConfigurationHandler" />

        <variable
            name="tunnels"
            type="org.amnezia.awg.databinding.ObservableKeyedArrayList&lt;String, ObservableTunnel&gt;" />
    </data>

    <androidx.coordinatorlayout.widget.CoordinatorLayout
        android:id="@+id/main_container"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="?attr/colorSurface"
        android:clipChildren="false">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical">

            <!-- Always-visible header: status, server picker, connect button, logo -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:gravity="center"
                android:orientation="vertical"
                android:paddingTop="20dp"
                android:paddingBottom="16dp">

                <LinearLayout
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:gravity="center_vertical"
                    android:orientation="horizontal">

                    <ImageView
                        android:id="@+id/connection_status_dot"
                        android:layout_width="10dp"
                        android:layout_height="10dp"
                        android:layout_marginEnd="6dp"
                        android:src="@drawable/ic_status_dot"
                        tools:tint="@color/connect_state_disconnected" />

                    <TextView
                        android:id="@+id/connection_status_text"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:textAppearance="?attr/textAppearanceTitleMedium"
                        tools:text="@string/disconnected"
                        tools:textColor="@color/connect_state_disconnected" />
                </LinearLayout>

                <!-- Server label + premium badge on same row -->
                <LinearLayout
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="4dp"
                    android:gravity="center_vertical"
                    android:orientation="horizontal">

                    <TextView
                        android:id="@+id/current_server_label"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:background="?attr/selectableItemBackground"
                        android:clickable="true"
                        android:focusable="true"
                        android:onClick="@{() -> fragment.onServerPickerClicked()}"
                        android:padding="8dp"
                        android:textAppearance="?attr/textAppearanceBodyMedium"
                        android:textColor="?attr/colorPrimary"
                        tools:text="Server: ⚡ Auto (Fastest)" />

                    <!-- PREMIUM badge — visible only for paid/vip tier -->
                    <TextView
                        android:id="@+id/premium_badge"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:layout_marginStart="4dp"
                        android:background="@drawable/bg_premium_badge"
                        android:paddingStart="8dp"
                        android:paddingEnd="8dp"
                        android:paddingTop="3dp"
                        android:paddingBottom="3dp"
                        android:text="@string/erawan_premium_badge"
                        android:textAppearance="?attr/textAppearanceLabelSmall"
                        android:textColor="@color/erawan_gold_light"
                        android:visibility="gone"
                        tools:visibility="visible" />

                </LinearLayout>

                <!-- Speed display — visible only while tunnel is UP -->
                <TextView
                    android:id="@+id/speed_text"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="2dp"
                    android:fontFamily="monospace"
                    android:textAppearance="?attr/textAppearanceLabelMedium"
                    android:textColor="?attr/colorOnSurfaceVariant"
                    android:visibility="gone"
                    tools:text="↓ 2.4 MB/s  ↑ 0.1 MB/s"
                    tools:visibility="visible" />

                <LinearLayout
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="12dp"
                    android:gravity="center_vertical"
                    android:orientation="horizontal">

                    <com.google.android.material.button.MaterialButton
                        android:id="@+id/connect_button"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:onClick="@{() -> fragment.onConnectClicked()}"
                        android:text="@string/connect_button" />

                    <com.google.android.material.progressindicator.CircularProgressIndicator
                        android:id="@+id/connect_progress"
                        android:layout_width="28dp"
                        android:layout_height="28dp"
                        android:layout_marginStart="14dp"
                        android:indeterminate="true"
                        android:visibility="gone"
                        app:indicatorSize="28dp"
                        tools:visibility="visible" />
                </LinearLayout>

                <androidx.appcompat.widget.AppCompatImageView
                    android:id="@+id/logo_placeholder"
                    android:layout_width="120dp"
                    android:layout_height="120dp"
                    android:layout_gravity="center"
                    android:layout_marginTop="20dp"
                    android:layout_marginBottom="4dp"
                    android:src="@mipmap/ic_launcher_foreground" />

                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_gravity="center"
                    android:layout_marginStart="@dimen/tunnel_list_placeholder_margin"
                    android:layout_marginEnd="@dimen/tunnel_list_placeholder_margin"
                    android:text="@string/tunnel_list_placeholder"
                    android:textSize="18sp" />
            </LinearLayout>

            <!-- Tunnel list — weight=1 so it takes ALL remaining space above the banner.
                 Banner never overlaps these rows. -->
            <androidx.recyclerview.widget.RecyclerView
                android:id="@+id/tunnel_list"
                android:layout_width="match_parent"
                android:layout_height="0dp"
                android:layout_weight="1"
                android:clipToPadding="false"
                android:paddingBottom="8dp"
                android:visibility="@{tunnels.size() > 0 ? android.view.View.VISIBLE : android.view.View.GONE}"
                app:configurationHandler="@{rowConfigurationHandler}"
                app:items="@{tunnels}"
                app:layout="@{@layout/tunnel_list_item}"
                tools:itemCount="4"
                tools:listitem="@layout/tunnel_list_item" />

            <!-- Compact gold-premium banner — dark-gold card + bright gold border,
                 sits BELOW RecyclerView so tunnel rows are never covered. Free tier only. -->
            <LinearLayout
                android:id="@+id/upgrade_banner"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginStart="12dp"
                android:layout_marginEnd="12dp"
                android:layout_marginBottom="72dp"
                android:background="@drawable/bg_upgrade_banner"
                android:gravity="center_vertical"
                android:orientation="horizontal"
                android:paddingStart="14dp"
                android:paddingEnd="6dp"
                android:paddingTop="10dp"
                android:paddingBottom="10dp"
                android:visibility="gone">

                <LinearLayout
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:orientation="vertical">

                    <TextView
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:text="@string/erawan_upgrade_banner_title"
                        android:textAppearance="?attr/textAppearanceLabelLarge"
                        android:textColor="#F6D98A"
                        android:textStyle="bold" />

                    <TextView
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:layout_marginTop="1dp"
                        android:text="@string/erawan_upgrade_banner_subtitle"
                        android:textAppearance="?attr/textAppearanceLabelSmall"
                        android:textColor="#C8A45A" />

                </LinearLayout>

                <!-- Gold gradient button with dark text — prominent + tappable -->
                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:background="@drawable/bg_upgrade_btn"
                    android:clickable="true"
                    android:focusable="true"
                    android:onClick="@{() -> fragment.onUpgradeClicked()}"
                    android:paddingStart="14dp"
                    android:paddingEnd="14dp"
                    android:paddingTop="7dp"
                    android:paddingBottom="7dp"
                    android:text="@string/erawan_upgrade_cta"
                    android:textColor="#2a1c05"
                    android:textSize="11sp"
                    android:textStyle="bold" />

            </LinearLayout>

        </LinearLayout>

    </androidx.coordinatorlayout.widget.CoordinatorLayout>
</layout>
